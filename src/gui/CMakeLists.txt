# A GUI entra na Fase 5 (ImGui + GLFW via FetchContent). O alvo fica declarado para que a
# opção CABRAL_BUILD_GUI exista desde já sem quebrar a configuração.
message(STATUS "cabral: GUI target is deferred to phase 5")
